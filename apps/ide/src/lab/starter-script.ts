/*
 * The canonical starter bot, imported rather than copied.
 *
 * `nova factory` scaffolds this same file into every new project and the
 * builder manual walks through it, so a second copy here would drift — and the
 * drift would be invisible until someone compared a scaffolded bot against the
 * one the lab seeds.
 */
import starterScript from '@morten-olsen/nova-docs/examples/starter-builder.js?raw';

export { starterScript };
